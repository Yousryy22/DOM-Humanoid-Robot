Tasks:
update the code with motion functions to handle servos according to the motion commands the code is subscribing to from ROS/python
example:
if recieved MOVE_FORWARD from ros: start executing forward function
if recieved MOVE_RIGHT from ros: start executing right function
( EXECUTION MUST BE IN ORDER ) 
